if [[ ${AUTO_UPDATE} == 1 ]] && [[ -d .git ]]; then git stash save --keep-index; git pull https://${USERNAME}:${ACCESS_TOKEN}@${GIT_ADDRESS} ${INSTALL_BRANCH}; fi; if [[ ${AUTO_UPDATE} == 1 ]] && [[ ! -d .git ]]; then git clone https://${USERNAME}:${ACCESS_TOKEN}@${GIT_ADDRESS} ${INSTALL_BRANCH} /home/container/; fi; if [[ ! -z ${NODE_PACKAGES} ]]; then /usr/local/bin/npm install ${NODE_PACKAGES}; fi; if [ -f /home/container/package.json ]; then /usr/local/bin/npm i; fi; npx pm2 start ecosystem.config.js --watch; npx pm2 logs;